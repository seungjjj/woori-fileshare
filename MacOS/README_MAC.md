# 🍎 Woori 파일 공유 프로그램 - macOS 버전

## 🚀 빠른 시작 (3단계)

### 1️⃣ 터미널 열기
- **Spotlight 검색** (⌘ + Space) → "터미널" 입력 → Enter
- 또는 **Finder** → 응용 프로그램 → 유틸리티 → 터미널

### 2️⃣ 프로젝트 폴더로 이동
```bash
cd /경로/파일공유프로그램/V3
```

### 3️⃣ 초기 설정 실행 (최초 1회만)
```bash
chmod +x setup_mac.sh
./setup_mac.sh
```

---

## 💻 프로그램 실행

### 서버 실행
```bash
./run_server_mac.sh
```

### 클라이언트 실행
```bash
./run_client_mac.sh
```

---

## 📦 .app 파일 생성 (배포용)

### 빌드 실행
```bash
./build_mac_apps.sh
```

### 생성된 파일
- `dist/Woori_Server.app` - 더블클릭으로 실행 가능
- `dist/Woori_Client.app` - 더블클릭으로 실행 가능

### .app 파일 배포
1. `dist` 폴더의 `.app` 파일을 다른 Mac에 복사
2. 더블클릭하여 실행
3. 보안 경고 시:
   - **시스템 환경설정** → **보안 및 개인 정보 보호**
   - "확인 없이 열기" 클릭

---

## 🌐 크로스 플랫폼 사용

### ✅ Mac 클라이언트 → Windows 서버 접속 가능!

**예시:**
1. Windows PC에서 서버 실행 → Cloudflare URL 생성
   ```
   예: https://abc-def-ghi.trycloudflare.com
   ```

2. Mac에서 클라이언트 실행 → URL 입력
   ```
   접속키: https://abc-def-ghi.trycloudflare.com
   아이디: admin
   비밀번호: admin
   ```

3. 접속 완료! 파일 다운로드/업로드 가능

### 🔄 모든 조합 가능
- ✅ Windows 서버 ↔ Mac 클라이언트
- ✅ Mac 서버 ↔ Windows 클라이언트
- ✅ Mac 서버 ↔ Mac 클라이언트
- ✅ Windows 서버 ↔ Windows 클라이언트

---

## 🔧 문제 해결

### "권한이 거부되었습니다" 오류
```bash
chmod +x *.sh
```

### Python이 없다고 나올 때
```bash
# Homebrew 설치 (없다면)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Python 설치
brew install python@3.11
```

### PyQt5 설치 실패
```bash
# Qt 의존성 설치
brew install qt@5

# 가상환경에서 재설치
source venv/bin/activate
pip install PyQt5 --no-cache-dir
```

### .app 실행 시 "손상되었습니다" 오류
```bash
# 터미널에서 실행
xattr -cr dist/Woori_Server.app
xattr -cr dist/Woori_Client.app
```

### 방화벽 문제
1. **시스템 환경설정** → **보안 및 개인 정보 보호**
2. **방화벽** 탭
3. **방화벽 옵션...**
4. Python 또는 앱 허용

---

## 📂 파일 구조

```
V3/
├── MacOS_Setup_Guide.md      # 자세한 설정 가이드
├── README_MAC.md              # 이 파일
├── requirements_macos.txt     # Python 의존성
├── setup_mac.sh              # 초기 설정 스크립트 ⭐
├── run_server_mac.sh         # 서버 실행 스크립트
├── run_client_mac.sh         # 클라이언트 실행 스크립트
├── build_mac_apps.sh         # .app 빌드 스크립트
├── build_server_mac.spec     # 서버 빌드 설정
├── build_client_mac.spec     # 클라이언트 빌드 설정
├── unified_server_pyqt.py    # 서버 소스 코드
├── gui_client_pyqt.py        # 클라이언트 소스 코드
├── server.py                 # Flask 서버 모듈
├── cloudflared_manager.py    # 터널 관리자
└── venv/                     # 가상환경 (자동 생성)
```

---

## 🎯 주요 기능

### 서버 (Server)
- ✅ 여러 폴더 공유 가능
- ✅ 사용자 계정 관리
- ✅ Cloudflare 터널 자동 생성 (원격 접속)
- ✅ 파일 업로드 받기 지원
- ✅ 시스템 트레이 최소화

### 클라이언트 (Client)
- ✅ 파일 다운로드
- ✅ 폴더 다운로드 (ZIP 또는 압축 해제)
- ✅ 파일/폴더 업로드 (실시간 진행률)
- ✅ 서버 저장 기능
- ✅ 다중 파일 선택

---

## 📞 도움말

### 자세한 설정 가이드
```bash
open MacOS_Setup_Guide.md
```

### 터미널 명령어
```bash
# 가상환경 수동 활성화
source venv/bin/activate

# 가상환경 비활성화
deactivate

# 패키지 재설치
pip install -r requirements_macos.txt

# Python 버전 확인
python3 --version
```

---

## 🎨 스크린샷

### 서버
- 다크 테마 UI
- 사용자/폴더 관리
- 접속키 생성 및 복사

### 클라이언트
- 파일 브라우저
- 업로드/다운로드 진행률
- 다중 선택 지원

---

## 📝 참고사항

1. **첫 실행 시 설정**
   - `setup_mac.sh` 실행 (최초 1회)
   - 의존성 자동 설치

2. **일반 실행**
   - `run_server_mac.sh` 또는 `run_client_mac.sh`
   - 가상환경 자동 활성화

3. **.app 파일**
   - 더블클릭으로 실행 가능
   - 배포 시 복사만 하면 됨
   - 보안 경고는 시스템 환경설정에서 허용

4. **Windows와 호환**
   - 서버/클라이언트 모두 크로스 플랫폼
   - HTTP/HTTPS 기반으로 통신

---

## 🌟 버전

- **Version:** 1.0.0
- **Framework:** PyQt5
- **Python:** 3.8+
- **Platform:** macOS 10.14+

---

## 📄 라이선스

내부 사용을 위한 프로그램입니다.

---

**즐거운 파일 공유 되세요! 🚀**
