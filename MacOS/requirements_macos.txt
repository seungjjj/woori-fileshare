# macOS용 Python 의존성 패키지
# Python 3.8 이상 필요

# GUI 프레임워크
PyQt5>=5.15.0

# 네트워크 요청
requests>=2.28.0
urllib3>=1.26.0

# 이미지 처리 (선택사항)
Pillow>=9.0.0

# 시스템 트레이 (선택사항)
pystray>=0.19.0

# 보안
werkzeug>=2.0.0

# 서버 (프로덕션용)
Flask>=2.0.0
waitress>=2.1.0

# 업로드 진행률 추적 (권장)
requests-toolbelt>=0.10.0

# PyInstaller (.app 빌드용, 선택사항)
pyinstaller>=5.0.0

# 추가 의존성
certifi>=2022.0.0
charset-normalizer>=2.0.0
idna>=3.0

# Cloudflare 터널 (자동 설치됨)
# cloudflared는 별도 설치: brew install cloudflare/cloudflare/cloudflared
